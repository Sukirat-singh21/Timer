# Local decorative photo

`study-desk-mobile.*` and `study-desk-desktop.*` are optimized local derivatives of this Unsplash source image:

https://unsplash.com/photos/c639042777db

The image is used only as a low-contrast decorative background for the timer hero. The CSS gradients and theme color remain the visual fallback if the photo is unavailable.
